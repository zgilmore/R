#inlcude <iostream> 

use namespace std;

int cor.test.somevsall(int N, int M, matrix[N][M]){

	M=
	
	double pvals [M][N]; double strength [M][N]

			//declare the empty matrices here, pvals and strength, perhaps combined
			
			//N == number of rows of input matrix
			//M == rows.size()
			
	for( int i =0; i < M; ++i){
		
		//row = rows[j];
		
		for( int j= 0; j < N; ++j)}{
		
		/*RInside::test = cor.test(unlist(mat[row,]),unlist(mat[i,0]), method=method);
		
		*/
		
		
}

int main(){

}